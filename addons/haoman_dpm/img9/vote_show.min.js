$(function(){	

	$('#sb').unbind('click').bind('click',function(){
		var key=$("#st").val();
		var cid=$("#cid").val();
		if (key=='') {
			show('请输入关键词！');
			return;
		}
		$.ajax({
			url : vote_show_list,
			type : 'post',
			dataType : "json",
			async : false,
			data : {key:key,cid:cid},
			success : function(data) {
				if(data.s=='0'){
					$('#list').html(data.list);
					vote_show();

					set_zoom();
					loading = false;
				}
				if(data.s=='1'){
					$('#list').html(data.list);
					show('查询不到，感谢支持！');
					loading = false;
				}
			},
			error : function(data, error) {
				show('系统忙，请稍后！');
			}
		});
	});
	
	function vote_show(){
		$(".vor").each(function(i){
		   var width=$(this).html();
		   var id=$(this).attr("rel");
		   $('#vs'+id).animate({width:width},2000);
		});
	}
	
	//显示提示
	function show(str){
		$('.result').html(str);
		$('.result').show();
		setTimeout(function(){
			$('.result').hide();
		}, 2000);
	}
	
	//缩放操作
	// function set_zoom(){
	// 	$('.p').click(function (){
	// 		var img=$(this).attr("data-original");
	// 		var name=$(this).parents('.show').find(".na").html();
	// 		$('.show_zoom').html('<img src="'+img+'">');
	// 		$('.title_zoom').html(name);
	// 		$('.zoom').show();
	// 		$('.pinch-zoom').each(function () {
	// 			new RTP.PinchZoom($(this), {});
	// 		});
	// 	});
	// }
	
	// $('.zoom').click(function (){
	// 	$('.zoom').hide();
	// });
	
	//var h=$(window).height();
	var h=document.body.clientHeight;
	$(".show_zoom").height(h);
});