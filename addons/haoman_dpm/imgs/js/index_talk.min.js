$(function () {
    function t(t) {
        var n = $(this).scrollTop(), i = $(document).height(), l = $(this).height();
        n + l + 200 >= i && (e || (e = !0, $(".loading").show(), $.post(get_moretaleUser, {page: ++a}, function (t) {
            $(".user_panel ul").append(t), e = !1, $(".loading").hide()
        }, "json")))
    }

    $("#header_panel").clone().addClass("head_scoll").attr("id", "header_scoll").insertAfter($("#header_panel"));
    var e = !1, a = 0;
    $(".user_panel").on("click", "li", function () {
        location.href = go_talk +'&uid='+ $(this).attr("uid")
    });
    $(window).scroll(t), $(".left_panel .menu").click(function () {

        $(this).parent().parent().toggleClass("active")
    });
    $(".stealth").click(function () {
        $.sync_post("do=Stealth", {show: $(this).hasClass("sethide")}, function (t) {
            $.tip(t.data), $(".left_panel ul").removeClass("active"), 1 == t.code ? ($(".stealth").text("隐身"), $(".stealth").last().text("上线"), $(".stealth").addClass("sethide")) : ($(".stealth").text("在线"), $(".stealth").last().text("隐身"), $(".stealth").removeClass("sethide"))
        })
    });
    $("#image_upload").upload_image(function (t) {
        $.getJSON("do=EditInfo", {imgId: t}, function (t) {
            $.loadingModal(!1), t.code == -2 && $.alert(t.data), 1 == t.code && $.alert("", t.data, function () {
                location.reload()
            })
        })
    });
    $(".change_nickname").click(function () {
        var t = prompt("请输入您的新昵称");
        null != t && "" != t && $.sync_post("&do=nickname", {name: t}, function (t) {
            $.tip("修改成功"), location.reload()
        })
    });
    setInterval(function () {
        $.getJSON(get_unread, function (t) {
            1 == t.code && t.data > 0 ? $(".unread b").show().text(t.data) : $(".unread b").hide()
        })
    }, 1e4)
});